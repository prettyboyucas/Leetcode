#include <iostream>
#include <string>
#include <stack>


class Solution {
public:
	bool isValid(string s) {
		stack<char> cstack;
		for (auto& ch : s) {
			if (ch == '(' || ch == ')' || ch == '[' || ch == ']' || ch == '{' || ch == '}') {
				if(cstack.em)
			}
		}
	}
};